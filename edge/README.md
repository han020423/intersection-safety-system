# Edge

라즈베리파이에서 실행되는 코드 폴더입니다.

## 목표 기능
- 카메라 입력 기반 추론
- 우회전 FSM 판단
- LED / 디스플레이 경고
- 서버로 이벤트 전송

## 폴더 설명
- `inference/` : 모델 추론 실행
- `fsm/` : 상황 판단 로직
- `hmi/` : LED, 부저, 디스플레이 제어
- `communication/` : 서버 API 호출

## 1차 목표
- 우회전 위험 상황에서 STOP / CAUTION / GO 출력
- 위험 발생 시 서버에 이벤트 전송