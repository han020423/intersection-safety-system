# AI

학습 관련 코드와 설정을 관리하는 폴더입니다.

## 목표 기능
- 데이터셋 구성
- 객체 검출 모델 학습
- 모델 평가
- 라즈베리파이용 모델 변환

## 폴더 설명
- `configs/` : 데이터셋 설정 파일
- `scripts/` : 학습 / 평가 / 변환 스크립트
- `sample_data/` : 구조 확인용 소량 샘플

## 주의
- 원본 데이터셋은 Git에 올리지 않는다.
- 학습된 weight 파일은 Git에 올리지 않는다.
- 현재는 문서와 설정 중심으로 시작하고, 추후 스크립트를 추가한다.